{
  "name": "name_check",
  "version": "1.0.0",
  "description": "check name socckerway team with else site squad list",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -build",
    "watch": "ts-node -w src/index.ts",
    "start": "node dist/index.ts",
    "fresh": "rm -rf node_modules && yarn install",
    "type-check": "tsc --build tsconfig.json",
    "precommit": "yarn run type-check",
    "release": "yarn build && changeset publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Dron84/name_check.git"
  },
  "author": "dron84@gmail.com",
  "license": "ISC",
  "dependencies": {
    "@types/latinize": "^0.2.15",
    "latinize": "^0.5.0"
  }
}
